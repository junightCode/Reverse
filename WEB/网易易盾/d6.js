function sample(i, e) {
    var t = n
        , r = i[t(2220)];
    if (r <= e) {
        return i;
    }
    for (var a = [], o = 0, s = 0; s < r; s++) {
        s >= o * (r - 1) / (e - 1) && (a.push(i[s]),
            o += 1);
    }
    return a;
}


function getDate(token,x) {
    traceData = []
    let u = token
    for (let i = 0; i <= 100; i++) {
        f = h(u, [Math.round(a["dragX"] < 0 ? 0 : a["dragX"]), Math["round"](a.clientY - a.startY), s["now"]() - a.beginTime] + "");
        traceData.push(f)
    }


    r = sample(traceData, 50)
    a = token
    o = p(h(a, parseInt(x, 10) / 300 * 100 + ""));
    data = JSON['stringify']({
        d: p(r['join'](":")),
        m: "",
        p: o,
        ext: p(h(a, 1 + "," + traceData.length))
    })
}